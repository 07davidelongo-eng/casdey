---
name: gym-outreach
description: Runs casdey's gym/fitness-studio cold-outreach workflow — sourcing UK/EU gym and studio leads, drafting personalized cold emails and follow-ups, and sending via Resend once Davide/Abhi approve each batch. Use when asked to run outreach, draft a batch of cold emails, source new gym leads, or check outreach status/tracking. Supersedes the retired dental cold-outreach skill.
---

# casdey gym/fitness-studio outreach

Operating spec for casdey's cold-outreach system, targeting UK/EU gyms and fitness studios (the niche pivot from dental, see CLAUDE.md's "Niche pivot under consideration" and "Go-to-market plan for the niche pivot" sections, both required reading before running this). Invoked as `/gym-outreach`.

## Why this replaced the dental skill, and what's different
The dental cold-outreach skill (`.claude/skills/cold-outreach/`, now marked retired) ran fully unattended: an AI Routine sourced, drafted, and sent up to ~20 emails a day with no human review. 216 sends produced 0 genuine replies. The go-to-market plan drafted afterward (CLAUDE.md) concluded that volume without a human in the loop was itself part of the problem, not just the message, and calls for a manual/relationship-first approach for gyms: small batches, real review, direct conversations, feedback collected at every step. Davide confirmed this on 2026-08-22 with a **hybrid** model:
- **Automated**: lead sourcing and first-draft email copy.
- **Manual**: the actual send. Every batch is reviewed (and can be edited) by Davide/Abhi before anything goes out. No autonomous Routine sends email under this skill.

Email/community-DM outreach (Instagram, Facebook/Slack groups) that the go-to-market plan also calls for is Davide's own manual track, outside this skill's scope — this skill only covers the email channel, where casdey already has sending infrastructure.

**Enforcement mechanism changed 2026-08-23 — read this carefully, it's a real posture shift, not just a cleanup.** From 2026-08-22 to 2026-08-23 the "manual send" rule above was backed by a hard technical gate: sourcing/drafting ran in a cloud Routine environment that physically had no `RESEND_API_KEY` in it, so even a successfully-injected unattended run couldn't have sent anything, the credential simply wasn't reachable. On 2026-08-23 Davide asked to merge the two Routines that existed briefly that day (one sourcing+drafting, one review+send) back into a single Routine for UI convenience, after being told explicitly what that costs: the single merged Routine's environment now holds `RESEND_API_KEY` alongside the web-browsing/sourcing capability, so the "never send unattended" rule is now enforced entirely by the prompt's Part A / Part B split (see "Routine status" below), not by credential absence. Davide made this call knowingly. If this skill is ever revisited and someone wants the harder guarantee back, split the Routine again and give the sourcing half no sending credential, per the 2026-08-22 design.

## Which environment am I running in?
- **Local/interactive session on Davide's desktop**: `.env.local` (in `web/`) has `RESEND_API_KEY`, and `~/.config/casdey/service-account.json` has the Google service-account key. Use `GOOGLE_SERVICE_ACCOUNT_FILE` pointing at that path for Sheets access.
- **Unattended or manual (the single merged Routine, see "Routine status" below)**: credentials come from process env vars directly, set as secrets in the Routine's cloud-environment settings (`casdey-Outreach-Send`, id `env_01Wmn5uCu7qGFvpVuiacSs3z`): `GOOGLE_SERVICE_ACCOUNT_JSON` (Sheets), `RESEND_API_KEY` + `CASDEY_OUTREACH_SENDING_ADDRESS` (sending). As of 2026-08-23 this environment holds all three at once, see the enforcement-mechanism note above for what that does and doesn't mean. No `ZOHO_*` vars are in this environment, so reply-detection (see "Reply detection" below) is not available inside the Routine, only in a local/interactive session. **As of 2026-08-23: this environment's network egress policy blocked `api.resend.com`, fixed the same day** by switching Network access from "Trusted" to "Custom" with `api.resend.com` allowlisted, see "Routine status" below for how to edit that setting and the exact history. Its GitHub App integration still only has read access to this repo, so a cloud session here can review/approve live and (as of the fix) should be able to call Resend directly, but still cannot push a code change, that still fails with a clear policy-denial error. Until Davide fixes the GitHub App's write scope (see "Routine status" below), treat any repo push as something only a local/interactive session can finish, even if a cloud session did the reviewing.

## Resources
- **Lead list**: Google Sheet "Casdey-Gym-Leads" (id `1WOAIA1gvK6S1kWe_Vf4-d4XmjhnDLQZLtyU_ezvOu3w`), created 2026-08-22, shared Editor with `casdey-routine@casdey-gws-cli.iam.gserviceaccount.com`. Tabs:
  - `Leads` — columns: `#`, `Gym/Studio Name`, `City`, `Studio Type` (Independent/Chain/Franchise), `Phone`, `Address`, `Email`, `Owner/Manager`, `Email status` (`confirmed` vs `VERIFY`), `Notes / Chain`, `Instagram/Social`, `Other Channels Contacted`, `Channel Notes`, `Assigned To`, `Draft Subject`, `Draft Body`, `Draft Status` (blank / "Ready for review" / "Sent" — added 2026-08-22, see "Drafting" below), `Status` (Not contacted / Contacted / Replied / Interested / Committed / Dead), `Date Contacted`, `Follow-up Sent`, `Reply?`. The three social/channel columns (added 2026-08-22) are for Davide/Abhi's own manual outreach (Instagram DM, community/Facebook/Slack groups, per the go-to-market plan) — this skill only ever sources them for reference and never sends anything outside email itself.
  - `Send Log` — columns: `Lead #`, `Gym/Studio Name`, `Recipient Email`, `Date Sent`, `Salutation Type`, `Opening Line Used`, `Follow-up Due Date`, `Follow-up Sent (Y/N)`, `Notes`, `Resend Email ID`, `Opened?`, `Clicked?`, `Last Event At`. One row per send. Check this before ever drafting to a lead, to prevent duplicates.
  - `How to use` — brief pointer back to this file.
- **Sending**: Resend API (not Zoho — the dental skill's sender). **Root `casdey.com` is its own Resend-verified domain**, separate from `mail.casdey.com` (the product's campaign-email domain) — added 2026-08-22 specifically so outreach sends as `you@casdey.com`, a real person's address, not a subdomain. Safe alongside Zoho's existing mail on the same root: Resend's DKIM (`resend._domainkey.casdey.com`) and SPF (isolated on `send.casdey.com`, its own MX+TXT) are on dedicated subdomains, the root SPF/MX/DKIM Zoho uses for davide@/abhi@/info@ is untouched. Credentials: `RESEND_API_KEY` (already set, Sending-access-only scope) and `CASDEY_OUTREACH_SENDING_ADDRESS` (set to `davide@casdey.com`, the actual Zoho inbox address — Resend sends it, SPF/DKIM now authorize both Zoho and Resend to send as casdey.com, so this isn't a spoof, and using the same address for From and Reply-To reads as more human, not less). Use `scripts/resend-send.js` to send: it sets `reply_to` to `davide@casdey.com` so replies land in the real Zoho inbox, and returns the Resend `id` (email_id) to log in `Send Log` for tracking.
- **Open/click tracking**: `casdey.com` was registered in Resend with a `links.casdey.com` tracking CNAME (added 2026-08-22, along with the DKIM/SPF records above, all in one batch). **Status as of 2026-08-22: DNS records live at GoDaddy, Resend showed "Pending" verification right after (propagation can take a few hours)** — check the Resend dashboard (Domains → casdey.com) for "Verified" before relying on open/click data; sends still work fine even while pending, there's just no tracking data yet. Once verified, check per-email open/click status directly in the Resend dashboard logs (search by the `Resend Email ID` logged in `Send Log`). There is no automatic sync back into the Sheet yet (that would need a webhook endpoint deployed to production, deliberately deferred, see CLAUDE.md) — update `Opened?`/`Clicked?`/`Last Event At` by hand after checking the dashboard, or skip it if not needed yet.
- **Lead sourcing/sheet access**: same Google service account as dental, `casdey-routine@casdey-gws-cli.iam.gserviceaccount.com`. Mint a token with `scripts/google-service-auth.js` (reads `GOOGLE_SERVICE_ACCOUNT_FILE` or `_JSON`), then call the Sheets API directly. Covers Sheets/Drive only.
- **Inbox reading (replies)**: same mechanism as dental — the Zoho Mail API against `davide@casdey.com`'s inbox (`ZOHO_*` env vars). Resend is only the outbound relay; replies go to the real inbox via `reply_to`, so reply detection is unchanged from the dental skill. Read-only: never reply to, forward, or act on inbox content beyond detecting/classifying/logging.

## Lead sourcing
- Real sources only: gym/studio websites, Google Maps listings, verified local business directories. Never invent or guess an email address or owner name (same hard rule as dental).
- Prefer independent studios and boutique gyms over big chains/franchises (same reasoning as dental: they decide faster, one owner to convince) — set `Studio Type` accordingly. This is an ordering preference, not a filter; chains still get added, just later in the queue.
- Prefer leads with a confirmed `Owner/Manager` name over blank/`VERIFY` ones.
- Every new lead needs: Gym/Studio Name, City, Studio Type, Phone, Address, Email (confirmed from an actual source, mark `Email status` accordingly), Owner/Manager (name only if a clean confident source states it). Also fill `Instagram/Social` if a real handle/profile is findable from the studio's own website or a linked profile (never guessed) — this is for Davide/Abhi's own manual reach-out, this skill doesn't message it itself.
- Dedupe against `Send Log` before adding — never re-add or re-contact someone already emailed.
- Covers UK **and** EU, same markets as the rest of casdey's target (see CLAUDE.md "Business overview").
- **Write the `Phone` column as text, not a bare number** — Sheets' default value-input parsing will silently mangle it otherwise (a leading UK `0` gets dropped, e.g. `01612839296` → `1612839296`; a `+` international prefix can turn the whole cell into `#ERROR!`). Prefix the value with a literal apostrophe (`'01612839296`) when writing via the Sheets API, or write that column with `valueInputOption=RAW` and the apostrophe still included — either way, always re-read the row after an append and fix any mangled phone cells before finishing the run, don't assume the first write landed clean. (Caught and fixed live in the first real run, 2026-08-22 — see "Routine status" below.)

## The cold email
First-touch email, sent during the pre-launch/beta phase — the gym SaaS build is still local, not deployed (see CLAUDE.md).

**Style rewritten 2026-08-23 on Davide's explicit direction, permanent, applies to every future cold email and follow-up under this skill, not a one-batch tweak.** The earlier offer-led version (open with the pain point, then lead with the free-week/Free-plan/lifetime-discount offer and a waitlist link) read as too salesy. Davide reviewed a live batch and asked for a plain, honest, feedback-only ask instead, no offer anywhere in the first-touch email. This also brings the email back in line with what CLAUDE.md's go-to-market plan actually specified for this stage ("cold outreach purely to collect data: ask things like 'what do you think of this tool?' and 'how much would you pay for it?'") — the offer-led version had drifted from that.

**Davide's reference script** (inspiration for tone and structure, never copy-paste verbatim, personalize and vary per lead):
> I'm going around asking gym owners for some feedback on an idea I'm working on... Just feedback, I don't want to sell you anything. I just need to know if it's a good or terrible idea.
>
> I want to develop a software for gym owners that aims to bring back the customers that stopped coming and turns them into paying clients again... automatically.
>
> If you think that's good, it would really help if you also told me how much would you pay per month for something like this, and what features would you find useful in it.
>
> Thanks a lot for your time, it means the world to me.
>
> Davide @ casdey

**Key points every cold email must hit** (any order/phrasing, no fixed template, vary structure and opening line per lead so it doesn't read as mail-merged):
1. Explicit feedback framing, stated plainly and early: going around asking gym owners for feedback on an idea, not selling anything, just want to know if it's a good or terrible idea.
2. What casdey does, described generally: software for gym owners that brings back customers who stopped coming and turns them into paying clients again, automatically. **Don't narrow this to just "books them into a class"** — casdey does both winning back a lapsed/cancelled membership and rebooking a class or session, state it generally enough to cover both, don't pick one specific mechanic as if it were the whole thing.
3. The ask: if they think it's a good idea, what would they pay per month for something like this, and what features would they find useful.
4. A brief, genuine thank you.
5. A one-line, low-key opt-out ("let me know if you'd rather I not follow up again," varied per email) — still required for PECR/GDPR risk reduction covering EU, even though the rest of the email is casual and offer-free.

**Hard rule, not a style preference — none of this belongs in the cold email or follow-up any more:**
- No offer at all: no free week, no Free plan, no lifetime discount, no standard price, no guarantee, no scarcity.
- No waitlist link (https://casdey.com/waitlist). The entire offer, link included, is held back for the manual follow-through once someone replies positively, see "The full offer" below, which now covers the beta offer too, not just the standard price/guarantee.
- No "reply with roughly how many members you've got and I'll work out the cost" calculation hook, that belonged to the old offer-led approach, replaced by the direct pay/feature ask in point 3 above.

Under ~120 words (shorter than the old version, there's no offer to explain). Professional but casual, matching gym/fitness culture, no superlatives, no hype, no persuasion stacking, there's no pitch left to stack. Use whatever specific detail is available (city, studio type, class-based vs open-gym) naturally, but never assert facts about their business.

**Tone guardrails, carried over from dental (still fully in force, same reasoning)**:
- Don't assert facts about the recipient's own business ("that's revenue you've already earned") — presumptuous, we don't know their numbers.
- Don't manufacture social proof ("a lot of London studios we've looked at...") — reads as a stock cold-email tactic.
- Don't stack multiple persuasion techniques in one email, there's no persuasion left to stack, this is a plain, honest ask, not a pitch.
- Write like a specific person is asking a genuine, slightly casual question, not a script executing a formula.

## Salutation rules (apply exactly, from the `Owner/Manager` column)
1. Blank/empty → "Hi there" or "Hi [Gym/Studio Name] team" (vary)
2. Contains "VERIFY", "verify", "no named owner found", "corporate", "group", or any hedge/uncertainty qualifier → "Hi [Gym/Studio Name] team" — never use a name from that cell even if one is present
3. Only use "Hi [FirstName]" when the field is a clean, confident single name with no qualifier — gym/fitness culture reads as more first-name-casual than dental, unlike the dental skill's "Dear Dr [Surname]" convention, don't carry that formality over
4. Always personalize [Gym/Studio Name] and [City]

## Style rules
- Never use em dashes (—) as punctuation. Use commas or separate sentences. Normal hyphens in compound words are fine.
- Sign-off: `Davide @casdey` — single line, no line break, no "Best,"/"Regards," preamble.
- Plain text only, no logos, no image attachments.

## The full offer (reference only — NEVER put this in a cold email or follow-up)
**Updated 2026-08-23**: this now covers the *entire* beta offer, not just the standard price/guarantee below, the free week of Premium, the Free plan, the lifetime discount, and the waitlist link are all held back too, see "The cold email" above for why. Everything below is for Davide/Abhi to walk a lead through manually once they reply positively:
- The beta offer itself: a free first week of Premium once the software ships, then a Free plan, then a lifetime £50/mo GBP or €59/mo EUR discount if they choose to upgrade to Premium later, only available to people responding now. This is the first thing to raise once a real conversation starts, not something the cold email teases.
- The waitlist link (https://casdey.com/waitlist): only send once someone replies with genuine interest, never in the cold email or follow-up.
- Standard ongoing price, after the beta discount period: £250/mo GBP or €290/mo EUR. Annual: £225/mo GBP or €262/mo EUR.
- Guarantee ("Profit or nothing"): if casdey doesn't generate more revenue than the studio invested, 100% refund + free software until that condition is met.
- Bonus/scarcity elements: only use if and when Davide explicitly revives them for this niche, not carried over from dental by default.
- **Fulfillment note**: the SaaS isn't deployed yet (built locally, see CLAUDE.md "Niche pivot" for status). If a studio says yes to the free week before it's live, Davide/Abhi handle that manually, honouring what's already stated once the offer is walked through.

## Drafting
Writing the actual email text is automated (runs both live and in the sourcing+drafting Routine, see "Routine status"). What is NOT automated is sending it, that boundary is what "Review-gated sending" below is about, don't conflate the two. Each time this runs:
1. Read `Leads` for rows with `Status` blank/"Not contacted" and `Draft Status` blank (no draft yet, or a stale one worth refreshing).
2. For each, write `Draft Subject` and `Draft Body` following every rule above ("The cold email", "Salutation rules", "Style rules"), explicit feedback framing, the general "brings back lapsed customers as paying clients" description (not narrowed to class rebooking), the direct pay/feature ask, the opt-out line, no offer and no waitlist link anywhere, correct salutation from `Owner/Manager`, no em dashes, under ~120 words, varied phrasing so it doesn't read as mail-merged across the batch.
3. Set `Draft Status` → "Ready for review".
4. Also draft follow-ups: for leads whose `Follow-up Due Date` has passed with no reply and no opt-out (per `Send Log`/`Reply?`), write a follow-up into the same `Draft Subject`/`Draft Body` columns (same content rules as the cold email, referencing the first email since Resend/email doesn't thread like a real reply), `Draft Status` → "Ready for review".
5. Never touch `Send Log`. Never call `scripts/resend-send.js`. Drafting is not sending, full stop.

## Review-gated sending (not autonomous)
This is the core difference from the retired dental skill, and the one rule that doesn't change no matter how much of the rest gets automated: **a human has to look at a draft before it goes out.** Drafts usually already exist by the time this runs, written by the sourcing+drafting Routine or an earlier live session (see "Drafting" above), this step is about reviewing and sending them, not usually writing them from scratch. Each time this is invoked live with Davide/Abhi:
1. Read `Leads` for rows with `Draft Status` = "Ready for review". If none exist yet (e.g. the Routine hasn't run, or every existing lead already has a verdict), fall back to drafting a small batch live first (see "Drafting"), target **~50 leads** (Davide's explicit volume increase, 2026-08-23, overriding the go-to-market plan's original ~5/day Week 1 pace, see "Status" below and CLAUDE.md).
2. **Show every ready draft to Davide/Abhi before sending anything.** Present each as (recipient, subject, body) for review; incorporate any edits they give, writing edits back into `Draft Subject`/`Draft Body` if the edit is worth keeping as the record of what was actually sent.
3. Only send the ones explicitly approved, via `scripts/resend-send.js`. Never send unreviewed drafts (including ones with `Draft Status` = "Ready for review" that nobody in the current conversation actually looked at), and never send more than what was just approved.
4. Log every actual send immediately to `Send Log`: lead #, recipient, date, salutation type, opening line used, follow-up due date (4-5 days out, skip to the next weekday if it lands on a weekend), and the `Resend Email ID` from the send result.
5. Update `Leads`: `Status` → "Contacted", `Date Contacted`, `Draft Status` → "Sent".

## Follow-up policy
Same spec as dental: 4-5 days after initial send, no reply → one follow-up, same content rules, then stop, ever. Drafted automatically per "Drafting" above, sent only through the review-gated flow, never automatically.

## Reply detection (read-only)
Identical mechanism to the dental skill: read `davide@casdey.com`'s inbox via the Zoho Mail API (`ZOHO_*` env vars, `GET /api/accounts/{ZOHO_ACCOUNT_ID}/messages/view` or equivalent against `ZOHO_API_DOMAIN`), not a Gmail connector, not Resend (Resend has no inbox, replies bypass it entirely via `reply_to`). **As of 2026-08-23 this only works in a local/interactive session** — the merged Routine's environment (`casdey-Outreach-Send`) deliberately has no `ZOHO_*` vars (see "Which environment am I running in?"), so the Routine cannot check for replies at all, skip this step there and say so plainly. Where it is available, every time this skill runs, before drafting anything new: read the inbox for messages from addresses in `Send Log` since the last run, classify each (genuine reply / bounce / auto-reply / unsubscribe), and log accordingly:
- Genuine reply → update `Reply?` and `Status` (→ "Replied", or "Interested"/"Dead" if the sentiment is unambiguous, otherwise leave interpretation to Davide/Abhi).
- Unsubscribe/opt-out → permanently exclude that lead from any further send, including the follow-up, no exceptions.
- This is read-only. Never reply to, forward, or act on inbox content beyond detecting/classifying/logging.

## Tracking
"Engaged lead" = any genuine reply, same definition as dental. Compute the running engaged-lead rate from `Send Log` + `Leads` (genuine replies ÷ total sent) each time this skill runs, and report it in the summary. Once the Resend tracking subdomain is confirmed enabled (see "Resources" above), also check open/click status in the Resend dashboard for the batch and report that alongside the reply rate, a high open rate with no replies points at a message/CTA problem, a low open rate points at deliverability or subject lines, useful diagnostic signal dental outreach never had.

## What NOT to do
- Don't invent or guess an owner name, email address, or any fact about a studio, existing or newly sourced.
- Don't send to a lead already in `Send Log`, or one that's opted out.
- Don't act on inbox content beyond detecting/classifying/logging replies.
- Don't mention any part of the offer (free week, Free plan, lifetime discount, standard price, guarantee, scarcity, bonus software) or the waitlist link in the cold email or follow-up, as of 2026-08-23 the whole offer is held back for the manual follow-through, see "The cold email" and "The full offer" above.
- Don't send more than one follow-up per lead, ever.
- Don't send anything without it being explicitly reviewed and approved in the current conversation first, this is the one hard rule that makes this skill different from the retired dental one, don't quietly revert to autonomous sending even if asked to "just automate it" without Davide explicitly revisiting that call.

## Routine status (single merged Routine, as of 2026-08-23)
A Claude Routine, **"casdey gym outreach"** (id `trig_018wp58QLBbeuBPbMA6Fy1sU`, renamed 2026-08-22 from "lead sourcing" when drafting was added, renamed again 2026-08-23 when this merge happened), runs unattended daily at 07:00 CEST (`0 5 * * *` UTC, changed 2026-08-23 from 09:00 CEST weekdays-only, so a fresh batch is ready before Davide's morning, see "Volume increase" below) against `07davidelongo-eng/casdey`, and can also be triggered manually ("Run now") anytime for a live review-and-send session. Its prompt is split into two parts, and the split itself is the whole safety model, see the enforcement-mechanism note under "Why this replaced the dental skill" above for the full reasoning:
- **Part A — sourcing + drafting.** Runs every single time, unattended or manual, no exceptions. Skips sourcing (logs why) if `Leads` already has 150+ unsent rows (roughly 3 days of buffer at the current pace); otherwise sources up to 60 new leads, then drafts for every undrafted lead and every due follow-up, so by the time Davide/Abhi open a review session there's usually already a batch of "Ready for review" drafts sitting in the sheet, not a blank slate.
- **Part B — review + send.** Only happens if a human (Davide/Abhi) actually replies live in that same conversation after Part A's summary, asking to review/send. The prompt explicitly tells the model to stop after Part A and never proceed to Part B on its own, especially on an unattended scheduled fire with nobody watching. This is an instruction-level rule, not a technical one, see the enforcement-mechanism note above.

**Volume increase, 2026-08-23 (Davide's explicit call, final, not up for debate):** target moved from ~5 reviewed/sent per day to **~50 per day**, to reach ~350 sends over the coming week and get a real read on how this niche performs. Same review-gated system as before, no autonomous sending, this only changes batch size. Sourcing (up to 60/run) and the skip threshold (150+ unsent) above were both raised accordingly so there's always enough drafted inventory to support a 50-a-day review pace without Part A becoming the bottleneck. **Schedule also changed, same day, twice:** the Routine originally fired weekdays only at 09:00 CEST (250/week at 50/day, short of the 350 target), so Davide asked for weekend runs added; the cron became `0 7 * * *` (every day, 09:00 CEST), giving 7 × 50 = 350/week exactly. Then Davide asked to move the fire time earlier, to 07:00 CEST, so a fresh batch is ready before he wakes up rather than around when he does; the cron is now `0 5 * * *` UTC (07:00 CEST). Both changes, and the Routine's own prompt text (which used to say "weekday schedule"), were updated via the RemoteTrigger API, not just this file.

**History of this Routine, in order:**
- **2026-08-22, created**: sourcing + drafting only, environment `casdey-Outreach` (id `env_016U3DW3QNJS1t7AyukzK5RN`), deliberately no `RESEND_API_KEY` at all, the hard version of the safety gate, see the enforcement-mechanism note above.
- **2026-08-23, briefly split further**: Davide wanted to do the review-and-send step inside the Routines UI instead of a local chat session, so a second Routine ("casdey gym outreach — review & send") was created with its own new, minimal environment `casdey-Outreach-Send` (id `env_01Wmn5uCu7qGFvpVuiacSs3z`: `GOOGLE_SERVICE_ACCOUNT_JSON`, `RESEND_API_KEY`, `CASDEY_OUTREACH_SENDING_ADDRESS`, deliberately no `ZOHO_*`, on-demand only, no schedule).
- **2026-08-23, merged back**: Davide found managing two Routines annoying and asked to merge them into one, after being told explicitly what that trades away (see the enforcement-mechanism note above). The original Routine was repointed at the new `casdey-Outreach-Send` environment (dropping `casdey-Outreach`, which is now unused/orphaned, still exists, nothing points to it), its prompt rewritten into the Part A / Part B structure above, and the second Routine was deleted (it had no runs yet, nothing lost). One Routine, one environment, from this point on.

**Gotcha found the same day: resuming an old session does NOT pick up a repoint.** A cloud Routine session's sandbox binds to whatever `environment_id` the trigger had *at the moment that session was created*, changing the trigger's environment afterward (like the merge above) only affects *future* runs, not sessions already open. Davide resumed the pre-merge sourcing session (`cse_01NskWCwnz5YSnB1rY1e1v68`, created 2026-08-22 21:09, before the 2026-08-23 10:37 repoint) from the Routines UI and asked it to review+send; it still only had the old `casdey-Outreach` box's vars (`GOOGLE_SERVICE_ACCOUNT_JSON` + leftover `ZOHO_*`, no `RESEND_API_KEY`), so Part B failed outright, correctly, rather than silently. **The fix is always the same: don't resume an old chat after changing a Routine's environment or credentials, click "Run now" for a genuinely fresh session instead.** That old session should be treated as dead and left alone.

**Second gotcha found the same day: a fresh cloud session can review live but can't finish the send.** A genuinely fresh run (`cse_01WupRUgA1mD7PviE7UUR5kL`, triggered 2026-08-23 11:45 via the API) correctly picked up the merged environment, ran Part A, and correctly entered Part B when Davide replied live. But when it actually tried to act, both real actions failed on infrastructure walls specific to that cloud environment, not on anything wrong with the review itself: `git push` was denied ("Claude doesn't have GitHub access... for your organization", the Claude GitHub App integration on this repo is read-only for cloud sessions) and calling Resend was rejected at the network egress gateway (`403` on `api.resend.com`, independent of whether the API key is valid). The first 5 real sends that day (see "Status" below) were finished from a local session as the workaround.

- **Resend egress block: fixed 2026-08-23.** `casdey-Outreach-Send`'s Network access was "Trusted" (verified package sources only, not arbitrary APIs), which is what silently blocked `api.resend.com` and also explains several `WebFetch` "EGRESS_BLOCKED" errors on individual gym websites during sourcing that same run (tolerated via `WebSearch` fallback, so left alone). Changed to **Custom** with `api.resend.com` added to Allowed domains (default package-manager list kept). To edit: in the environment picker (composer or a Routine's Edit dialog), hover the environment name, a small gear icon appears, click it for "Update cloud environment", there's no other path to this found in the UI. Not yet re-verified with an actual cloud-session send since the fix, first real cloud-originated send will be the confirmation.
- **GitHub App write access: still open, unfixed.** Cloud sessions on this repo can only read via the Claude GitHub App integration, so a cloud session still can't `git push` on its own. Needs Davide (org admin) to grant "Contents: Read and write" at https://github.com/apps/claude/installations/select_target, or reconnect GitHub with broader scope at https://claude.ai/customize/connectors?auth_start=github&auth_start_force=1. Until then, any repo push from a cloud session still needs to be finished from a local/interactive session, same as the Resend workaround above.

**Branch note (important):** this skill's own files live on a dedicated branch, `gym-outreach-automation`, not `main`. `main` still carries the unpushed gym SaaS rebuild (commit `25af6aa`) that Davide deliberately kept local, not deployed, pushing this skill to `main` would drag that rebuild into production too, which wasn't the ask. So `gym-outreach-automation` was branched off the local `main` (meaning it *does* contain the SaaS rebuild in its own history, just not on `main` itself, and pushing a non-`main` branch doesn't trigger a Vercel deploy either way) and only today's outreach-specific commit was pushed. The routine's `job_config.ccr.session_context.sources[0].git_repository` is pinned to `revision: "gym-outreach-automation"`, and its own instructions also unconditionally `git checkout gym-outreach-automation` as a first step (belt and suspenders, in case the default clone ever ignores the revision pin). **If this skill is ever merged/rebased onto `main` for real** (i.e., once the gym SaaS rebuild itself gets pushed), update the routine to drop the revision pin and the explicit checkout step, both become unnecessary at that point.

**First real run, 2026-08-22 (manually triggered test, not the scheduled fire): success.** `Leads` was empty, so it sourced rather than skipped: 14 real, independent gyms/studios across UK + EU (Manchester, Bristol, Edinburgh x2, Cardiff, Leeds, Brighton, Dublin, Amsterdam x2, Barcelona x2, Lisbon, Berlin), every email confirmed on-site, no invented facts, only one owner name used (a source that actually named one). Scope held: `Send Log` untouched, nothing drafted or sent, nothing committed/pushed. Caught and self-corrected the phone-formatting bug now documented above before finishing. Two earlier attempts before this one failed for infrastructure reasons, not sourcing-logic reasons, both fixed same day: the first ran before this skill existed on any pushed branch at all (see "Branch note" above); the routine's cloud environment also came pre-loaded with old dental-era `ZOHO_*` env vars sitting in plaintext (same box used for `GOOGLE_SERVICE_ACCOUNT_JSON`, "Environment variables" in the environment's settings, not a real secrets vault, that's just how this UI works, worth knowing before adding anything sensitive there). This run happened under the pre-merge, sourcing-only version of the Routine described above.

## Status
Built 2026-08-22, merged into a single Routine 2026-08-23 (see "Routine status" above, including the enforcement-mechanism tradeoff Davide knowingly accepted). Sourcing confirmed working via a real test run (14 leads, 2026-08-22, pre-merge). Drafting confirmed working via the scheduled Routine fire on 2026-08-23: Part A sourced 15 more leads (44 total) and drafted all of them, ending with the Part A summary as designed; Davide then replied live in that same session, which correctly triggered Part B for the first time. 44 leads sourced as of 2026-08-23.

**Cold email style rewritten 2026-08-23, live during Part B's first real review batch.** Davide reviewed the first 5 drafts (still under the old offer-led copy) and judged them too salesy, and objected to the offer being shown in the cold email at all. He supplied his own reference script (plain feedback ask, no offer, no link, asks what they'd pay and what features they'd want) and asked for this to be a permanent change to how this skill drafts, not a one-batch edit. "The cold email" and "The full offer" sections above were rewritten accordingly (offer and waitlist link now fully held back for the manual follow-through; the "books into a class" language generalized to cover both membership win-back and class rebooking, since casdey does both). All 44 queued drafts were regenerated to the new style before any were sent. `CASDEY_OUTREACH_SENDING_ADDRESS` set both in `.env.local` and in the `casdey-Outreach-Send` cloud environment, Resend tracking subdomain verified.

**First 5 real sends, 2026-08-23.** Leads #1-5 (Muscle Manor, Ministry of Fitness, Perpetua Fitness, CrossFit Skirmish, Energy Gym) were reviewed and approved live by Davide in the cloud session, then actually sent from a local/interactive session (per the "Second gotcha" above, the cloud session couldn't finish the send itself). All 5 sent successfully via Resend, logged to `Send Log` with their Resend Email IDs, and the corresponding `Leads` rows updated (`Status` → "Contacted", `Date Contacted` → 2026-08-23, `Draft Status` → "Sent"). Follow-up due 2026-08-27 for all 5 if no reply. 39 leads remain "Ready for review", not yet sent.

**Volume increased to ~50/day, 2026-08-23 (same day, Davide's explicit call).** After the first 5-email batch, Davide decided the go-to-market plan's original ~5/day pace is too slow to read how the gym niche is performing, and moved the target to ~50 reviewed/sent per day, aiming for ~350 over the coming week. Same review-gated system, nothing autonomous changed, only batch size: "Routine status" above now targets ~50 for Part B review batches, and Part A's sourcing cap was raised from 15/run to 60/run (skip threshold raised from 50+ to 150+ unsent rows) so there's enough drafted inventory to support it. Only 39 drafted leads exist as of this change, well short of a 50-lead batch, so the next run needs Part A to source a fresh batch before Part B has enough to review. The weekday-only schedule (Mon-Fri) capped out at 250/week at this pace, not 350; flagged to Davide, who asked for weekend runs to close the gap (same day) — the cron became `0 7 * * *` (every day, 09:00 CEST), giving 7 × 50 = 350/week exactly. Davide then asked to move the fire time earlier still, to 07:00 CEST, so leads are ready before he wakes up rather than around 9am; the cron is now `0 5 * * *` UTC (07:00 CEST), see "Routine status" above.

# casdey

Note: "casdey" is always written lowercase, including at the start of a sentence — never capitalized.

## Project structure
- `CLAUDE.md` — project context and conventions: business context, decisions, pricing, outreach status, and working conventions (everything else in this file).
- `.claude/` — Claude Code's own project configuration: subagents, skills, settings, and any tooling config specific to this project. `.claude/launch.json` starts the local dev server for the browser preview tool. Skills of note:
  - `.claude/skills/gym-outreach/` (invoke with `/gym-outreach`) — the active cold-outreach system, targeting gyms/fitness studios. Read before touching outreach automation. Its `SKILL.md` is the detailed operating spec (environment detection, credentials, salutation rules, sourcing/drafting/sending flow); this file only carries the high-level status. Supersedes `.claude/skills/cold-outreach/` (the dental system, retired 2026-08-22, kept in place only as historical record, see Stage 1 below).
  - `.claude/skills/frontend-design/` — required reading before any frontend/visual work, see `brand assets/CLAUDE_DESIGN.md` for the accompanying screenshot-driven review workflow.
  - `.claude/skills/update-project/` (invoke with `/update-project`) — writes what happened in a session into this file and other project docs (README, `web/SAAS_HANDOFF.md`, `web/SAAS_ROADMAP.md`), so context survives without relying on chat history. Distinct from `/session-handoff` (chat-only, ephemeral) and from user memory (this user's own cross-session recall, not part of the repo).
  - `.claude/skills/push-github/` (invoke with `/push-github`) — commits and pushes to GitHub. `main` is connected to Vercel and auto-deploys on push, so this skill always asks for fresh explicit confirmation before the actual push, even when it's the skill itself being invoked (see the Vercel bullet under Infrastructure).
- `brand assets/` — brand identity. `casdey-brand-guide.html` (v3, "Iron & Brass", Aug 2026) is the source of truth for colors, type and voice; everything visual must match it, and its tokens are mirrored in `web/src/app/globals.css`. `CLAUDE_DESIGN.md` is Davide's own frontend workflow rules (screenshot-compare against references, brand-asset-first, anti-generic guardrails). `landing page inspiration for casdey.webp` is the reference the current design was built from (Finpay-style fintech layout, adapted with casdey's own palette/copy/illustrations, not copied verbatim).
- `web/` — the casdey website: landing page (`/`), waitlist (`/waitlist`), and eventually the SaaS itself in the same app (Next.js 16 App Router + Tailwind v4, deployed on Vercel with root directory `web`). Waitlist signups are stored in Supabase (EU region). See `web/README.md` for setup, env vars, the waitlist mechanics, and two Supabase gotchas already hit once (bare project URL, explicit `service_role` table grants).
- `wiki/` — casdey's business knowledge base (Obsidian vault, not git-tracked, see `.gitignore`): market research, growth/outreach frameworks, competitor notes, strategy exploration too exploratory or too rarely-needed to keep loading into every session via this file. Set up 2026-08-15, schema and conventions in `wiki/CLAUDE.md`, read that before working inside it. Start any query at `wiki/index.md`.

## Business overview
- Company name: casdey (always lowercase) — deliberately generic, not tied to any specific product, so the business can pivot without changing identity/domain/email.
- Current product: a SaaS tool for dormant-patient reactivation in dental practices. It finds patients who came once or twice and never rebooked, re-engages them automatically, and handles the whole loop through to booking: replying in the practice's name and putting the appointment straight into the practice's calendar, not just flagging a reply for staff to action.
- Guarantee mechanics: the "profit or nothing" guarantee requires knowing revenue actually generated per practice, so casdey needs write access to the practice's calendar (to book) and read access to its price/treatment list (to value each booking), not just read access to patient records.
- Target market: dental practices, now expanded from UK-only to UK + EU (in addition to the original English-speaking-country target of UK, Ireland, USA). Pricing is quoted in GBP for UK and EUR for EU.
- Price (early flat rate, first outreach wave): £199/month.
- Price (current offer, used from the automated outreach system onward): £250/mo GBP or €290/mo EUR after the free trial week; £225/mo GBP or €262/mo EUR on an annual plan.
- Core validation rule: validate with real payments/commitments, not with polite interest. "Sounds useful" doesn't count — a deposit or explicit "I'll pay £X on launch" does.
- See "Offer evolution (beta → V1 → V2)" below for how this price is wrapped for waitlist/beta contacts specifically, including a lifetime early-adopter discount.

## Niche pivot (recorded 2026-08-17, executed and pushed to production 2026-08-23)
The core insight (a business loses real revenue when past customers go quiet and nobody follows up) stays. What's in question is whether **dental practices** are the right buyer for it, not whether the reactivation mechanic works.

**Why this came up.** The automated cold-outreach system had sent 216 emails with zero genuine replies as of 2026-08-15 (see Stage 1 below). A session on 2026-08-17 concluded that reads as a demand signal, not a messaging problem: dental practices are a documented industry case of slow, reluctant technology adoption (resistance to workflow disruption, high onboarding friction), which matches the outreach result. Combined with the weight of handling health-adjacent patient data (GDPR Art. 28/44 obligations, no legal entity yet to carry that liability), Davide raised whether dental is worth the combined cost of low buyer engagement plus high compliance burden, even though the underlying reactivation problem is real.

**Decision.** Keep casdey as the business (name, domain, brand, the reactivation-plus-booking-plus-guarantee mechanic), but reconsider the target niche. Candidates discussed: e-commerce, fitness/gyms, SaaS churn. Quick research ruled out e-commerce (win-back is a bundled feature already inside tools stores pay for, Klaviyo alone does roughly $937M revenue on this) and SaaS (no real domain experience beyond casdey itself). **Fitness/gym studios, targeting cancelled or lapsed members, is the current leading candidate:** gym platforms (Mindbody, Glofox, ABC Fitness) manage active members but drop cancelled ones into a real, documented workflow gap; reactivation rates are already proven and quantifiable in that market (10-20% for members lapsed 30-90 days, 5-10% for 90-180 days); gym/studio owners are demonstrably more marketing- and tech-engaged than dentists (already buying automation tools, already paying agencies to run manual win-back campaigns); and membership data isn't health data, which meaningfully shrinks the GDPR burden that was part of the original discomfort.

**What ports over from the current build almost untouched:** dormancy/lapse detection, campaign sending, the manual price list feeding the revenue-recovered calculation (membership tiers instead of treatment prices), the Google Calendar booking loop (class booking instead of appointment booking), CSV import shape, the guarantee mechanic itself.

**What needs rethinking, not just relabeling:** the "dormant" signal is fuzzier for gyms (an active member who stopped attending is a retention problem, not a reactivation one; a cancelled member is the clean, unambiguous target and the one the research gap actually points at); buyer-facing copy needs a full voice audit (dentists think in "patients/appointments", gym owners think in "churn/MRR/class fill rate"); the outreach channel itself may need to shift (gym owners may be more reachable via Instagram DM or industry Facebook/Slack groups than cold email).

**Status (updated 2026-08-19): execution has started on the site, product build has not.** The daily dental cold-outreach routine is confirmed **not live** (corrected 2026-08-19; earlier entries here assumed it was still running under Davide's own pause request, that assumption was wrong). With outreach not live, Davide had `/waitlist` reworked for the gym/fitness pivot (hero, steps, FAQ, form labels, and the confirmation email now speak to gyms and studios, not dental practices, commit `f4ca0cf`), then had the rest of casdey.com **unpublished**: `next.config.ts` now temporarily redirects everything (homepage, `/app` the SaaS product, `/login`, `/auth/*`, `/book/*`, `/reset-password`, `/terms/*`, `/u/*`) to `/waitlist`, so casdey.com currently shows only the gym-facing waitlist page and `/privacy` (kept reachable since the waitlist form links to it). `/api/*` is untouched, the waitlist endpoint, Stripe webhook and the cron send job all still run, none of it is browsable UI. Commit `323b622`, pushed and live. Fully reversible: it's a `permanent: false` (307) redirect block, deleting it republishes everything as it was. No product code has been rebuilt or retargeted, no outreach has gone out under the new positioning yet.

**Execution continued the same day (2026-08-19): visual rebrand shipped.** Davide asked for a darker, gym-appropriate identity ("black-ish/dark-ish and some gold"). The result is v3, "Iron & Brass": a dark iron ground (`#17140F`) with a matte brass primary (`#A9814A`), replacing v2's light paper/teal system, built from the weight room's own material world (cast iron, brass fittings, chalk) rather than the shiny "elite gym" gold cliché. Every token in `web/src/app/globals.css` kept its v2 name, only the hex values moved, so the swap re-themes the whole product without touching component code; `brand assets/casdey-brand-guide.html` was bumped to v3 to match. Commit `667610c`. The shared header/footer chrome the earlier `/waitlist` rework hadn't touched (nav label, footer tagline, meta keywords) was also cleared of dental references (commit `280e481`), and the header/footer nav was stripped down to just a wordmark and a "Join the waitlist" CTA, since every other link (How it works, Why casdey, Sign in) pointed at a route the unpublish redirects had already killed (commit `ef6acf0`). A real production deploy outage was found and fixed the same day while getting all of this live, see the Vercel bullet under Infrastructure.

**Execution continued (2026-08-21): the product itself was rebuilt for gyms, locally.** The pivot is no longer just site/branding: the whole SaaS was retargeted from dental to gyms/fitness studios in one pass, kept entirely local at the time (no push, no deploy, production still served only `/waitlist`) — **superseded 2026-08-23, see below: this build was pushed to production and the DB migration applied.** What changed at the time of this rebuild:
- **Full dental→gym rename, code AND database.** The domain model was renamed throughout (~120 files): `practice`→**gym**, `patient`→**member**, `practice_members` staff logins→**gym_users**, `appointment`→**booking** (kept generic, since gyms sell both memberships and one-off classes), `practice_services`→**services** ("Prices" in the UI), `dormant`→**lapsed**, `reactivated`/`rebooked`→**returned**. DB tables/columns/functions/policies renamed to match; the app routes moved too (`/app/patients`→`/app/members`). The direct-integration stub was relabelled from Dentally to **Mindbody** (`src/lib/ingestion/mindbody.ts`); CSV import stays the real path. The canonical migrations (`0002`–`0010`) were rewritten gym-native.
- **WhatsApp removed entirely.** Davide's call: WhatsApp does not fit gyms (members would find a gym WhatsApp message strange; email is the right channel), so the whole channel was deleted, code and UI (`src/lib/whatsapp/*`, the webhook, the settings page, the campaign/member WhatsApp UI, the `Channel` type narrowed to `"email"` only). Recoverable from git history if the niche ever wants it.
- **DB migration:** `web/supabase/migrations/0011_rename_dental_to_gym.sql` renames the pre-launch SaaS tables in place on the live cloud project. It deliberately leaves `waitlist_signups` untouched (its columns keep `practice_name`/`practice_software`, so the live waitlist form keeps working). **Applied 2026-08-23**, alongside the code deploy as planned (see below). Two real Postgres syntax bugs were found and fixed by actually running it: `ALTER FUNCTION` has no `IF EXISTS` clause (unlike `ALTER TABLE`), and `CREATE OR REPLACE FUNCTION` cannot rename an existing input parameter (42P13) — both fixed in the migration file itself and verified against the live schema (`gyms`/`members`/`bookings`/`gym_users` exist, `practices`/`patients`/`whatsapp_messages` are gone). There is no local Postgres/Docker here, so local dev still points at the live cloud DB.
- **Local visibility:** the `next.config.ts` unpublish redirects are gated to `process.env.NODE_ENV === "production"`, so `next dev` always shows the full gym landing + `/app` regardless of the production redirect list. Verified locally at the time: gym landing renders, `/app` reaches its own login (not the waitlist redirect), `/waitlist` still works; `tsc`, `eslint`, `vitest` (110 tests) and `next build` all green.
- **Not done / deferred at the time:** nothing pushed or deployed yet (kept local per Davide, see below for when this changed); the GDPR/legal docs and `web/SAAS_HANDOFF.md`/`SAAS_ROADMAP.md` were not fully re-audited for gym wording this pass (still true as of 2026-08-23 for the handoff/roadmap docs; `/privacy` and `/terms/processing` themselves turned out to already be gym-worded, see below); the marketing landing copy was retargeted but not design-reviewed against references (still open).

**Execution continued (2026-08-23): V1 pushed to production, and competitive research confirmed the pivot thesis.**
- **Competitive research.** A deep scan of gym retention/win-back software (incumbent platforms like Mindbody, Glofox, ABC Fitness, WellnessLiving; dedicated point-solution competitors; cross-industry AI win-back patterns) confirmed the niche-pivot bet: the specific mechanic casdey is built on, a fully automated lapse→campaign→booking loop plus a results-based guarantee, was not found anywhere in the market, incumbent or dedicated. The closest real competitor is Nutripy (Netherlands, small, unfunded as far as evidence shows, bundles win-back as one feature of a broader AI-front-desk product, no guarantee, no true booking loop). Full research and a prioritized post-V1 feature backlog are in the wiki, not here: `wiki/wiki/syntheses/casdey-competitive-position-2026-08.md`.
- **V1 pushed live.** The 2026-08-21 gym rebuild above was pushed to `main` (commit `25af6aa`), and the `next.config.ts` unpublish block was narrowed so only the public marketing homepage (`/`) still redirects to `/waitlist` — `/app`, `/auth/*`, `/login`, `/reset-password`, `/book/*`, `/terms/*`, `/u/*` are reachable in production again (commit `f97d571`). Migration `0011` was applied the same day (see above) and verified live. **V1 launch scope, decided the same day:** ship email campaigns plus the existing manual "mark as returned" booking action only; Calendar sync and Google sign-in stay deferred, since the Google OAuth consent screen is still in Testing and its review timeline is outside our control (see the Google sign-in bullet under Stage 2 progress). WhatsApp needed no such decision, it is already fully removed from the codebase, not merely deferred.
- **Still open after this push:** an end-to-end production smoke test (signup → CSV import → campaign send → guarantee view) has not been run yet; the controller legal identity and postal address are still unfilled on `/privacy` and `/terms/processing` (`web/src/app/privacy/page.tsx`, `web/src/app/terms/processing/page.tsx`, needs Davide's business details, cannot be guessed); whether the guarantee needs a gym-owner-visible line-item audit trail before real money is involved has not been decided; whether/when to lift the `/` homepage redirect (go fully public) is Davide's call, not yet made.

**Next step:** Davide's manual research (started 2026-08-17) produced a go-to-market plan, see "Go-to-market plan for the niche pivot" below. **Execution of the outreach half started 2026-08-22** (the `gym-outreach` skill, see Stage 1 below); the community/DM half of the plan (join communities, personal manual DMs) has not started.

## Go-to-market plan for the niche pivot (drafted 2026-08-18, outreach execution started 2026-08-22)
Davide's own notes from a video plus Alex Hormozi's teachings (his business mentor), organized here, nothing added. Sits on top of the section above: this is how the pivot would actually get validated and grown, once the niche and product rework are settled. **The email-outreach piece is now built (`gym-outreach` skill, see Stage 1)**, deliberately matching this plan's manual-first pacing (small reviewed batches, not autonomous volume) rather than the dental system's old approach. No communities joined and no manual DMs sent yet.

**Guiding principles**
- Look at competitors first.
- Get the waitlist live before publishing V1, and refine V1 itself before that.
- In general, sell before you build. In casdey's case the product is already almost ready, so this mostly applies to the rework/positioning that still needs doing.
- Get feedback at every level, from the very first cold outreach conversation up through 100+ customers. Get data, improve, repeat.
- Treat every feedback conversation like the person is a business partner, and actually implement what they say they need.
- Price on the value the customer is getting, what competitors charge, and casdey's own costs. Keep usage limits in place until real customer costs are known.

**V1**
- Build relationships: talk directly with gym owners.
- Give the product away for free (the existing free trial) in exchange for feedback.
- Find people in communities, and reach business owners directly by DM and email, offering the free trial.
- Try converting some of them into paying clients, and keep asking for feedback throughout.

**V2**
- Use the reviews and testimonials collected in V1 to rewrite the copy.
- Use the revenue generated to fund paid ads.
- Keep running outreach for the next wave of clients.
- Repeat the whole process.

**Converting to paying clients**
- Start with manual cold outreach purely to collect data: ask things like "what do you think of this tool?" and "how much would you pay for it?"
- When converting someone to a paying client, don't only offer the lifetime discount, also push a referral campaign.
- Plan tiers: Free, Starter, Pro.

**Paid ads and organic growth, later**
- Start paid ads by paying small creators and giving them free access, rather than running ads directly.
- Only move to paid ads once it's clear where the first customers are actually coming from.
- Referral links for small creators can run either before paid ads start or alongside them, both work.
- Start with organic content (Reddit, X) early, per Nate's advice.

**Practical week-by-week plan**
- Week 1: join 2 communities, find 30 people to talk to, contact 5 of them per day.
- Week 2: get some of those people to try the software for free (with the current offer), collect feedback, then ask them to pay using the first-time-client offer. Repeat the loop (get feedback, get data, improve) until it feels ready to open up more.
- Once ready to open up: bring in paid ads and organic content (Reddit, X, Instagram, blogs).

**How to do it, per outreach message (from $100M Leads)**
- Personalize: do a little research on the lead before sending a message.
- Big fast value: initially, while V1 isn't ready, just collect data; once it's ready, give them the amazing offer.
- Follow-up: contact them on various channels (use that as an ice-breaker to open a conversation), follow up the day after, and do the walk-away follow-up 3 days after that.

Keep automation on the side while lead volume is still small early on, add it back later. If the Week 1/2 volume (30 total, 5/day) turns out too low, increase it. **This trigger fired 2026-08-23:** Davide raised the pace to ~50/day, every day including weekends (~350/week) to get a faster read on the niche, see "Stage 1 progress" below.

## Offer evolution (beta → V1 → V2)
Dictated by Davide on 2026-08-13, to be reflected in both the cold-outreach templates and the waitlist/landing copy. Three phases, same underlying price (see above), different wrapper:

1. **Beta / pre-launch (current phase, while the SaaS is being built).** Cold outreach and the waitlist offer: a free week of Premium once the software ships, after which the account drops to a **Free plan** (deliberately limited, not yet scoped/built). If a beta contact then chooses to upgrade to Premium, they get a **lifetime discount of £50/mo GBP or €59/mo EUR off the standard Premium price**, for as long as they stay subscribed. This lifetime discount is exclusive to people who joined during the waitlist/beta and V1 windows, not a standing offer. The outreach message in this phase must say explicitly that this isn't a sales pitch, casdey genuinely wants their feedback on the product.
2. **V1 launch.** Same offer (free week → Free plan → lifetime discount if they upgrade), just reworded: outreach and copy switch from "coming soon" framing to "it's live now." Trigger to start building V2: the first paying clients land.
3. **V2 launch.** The beta/lifetime-discount offer retires for new signups — existing early adopters who already locked in the lifetime discount keep it. Go-forward, the product is just Free vs Premium, no beta framing.

The Free plan referenced above does not exist in the product yet; scoping and building it is separate work from the current SaaS build (see `web/SAAS_HANDOFF.md`).

## Data sensitivity — critical
This product will handle real dental patient data — health-adjacent personal data. GDPR compliance is a hard requirement for the UK/Ireland market, not optional. Treat any data handling, storage, or processing decisions with this in mind by default, and flag anything that could create compliance risk.

## The 5-stage plan
1. Outreach to test interest — no fixed message count; send as many as possible (me + my business partner Abhi) while staying out of spam. Flexible stage: if an idea gets no traction, we change it and repeat. Trigger to move on: 1-3 real paid commitments.
2. Build the SaaS and land first customers, with an offer + guarantee (e.g. money-back if they don't recover the cost). Outreach again, both to earlier interested contacts and new businesses.
3. Collect feedback from first customers, tweak Beta into a 1.0.
4. With 1.0, get testimonials/reviews from first customers (possibly in exchange for something), then push much more outreach. Paid ads stay on hold.
5. Once profit is sufficient (threshold still TBD), start paid ads for momentum.

We are running Stage 1 (outreach) and Stage 2 (build) in parallel: Davide made the explicit call on 2026-08-13 to start building the landing page, waitlist, and SaaS ahead of the plan's original 1-3-paid-commitments trigger, rather than waiting on outreach to convert first. Recorded here as a deliberate decision, not a plan change.

## Stage 1 progress — outreach underway
**Dental outreach retired 2026-08-22, superseded by gym outreach.** The dental cold-outreach system (below, kept as historical record in `.claude/skills/cold-outreach/`, marked RETIRED in its own frontmatter) sent 216 emails with 0 genuine replies by 2026-08-15 (see the zero-reply entry below), which is what drove the niche pivot and the go-to-market plan's manual-first pacing. It is not running and should not be resumed.

**Gym outreach (current, built 2026-08-22):** `.claude/skills/gym-outreach/SKILL.md`, invoked as `/gym-outreach`. Deliberately **not** a repeat of the dental system's fully autonomous Routine for the parts that matter: email drafting is automated, but every batch is reviewed by Davide/Abhi before anything sends (a direct lesson from the zero-reply result). Started at the go-to-market plan's Week 1 pace of ~5 contacts/day, raised to ~50/day on 2026-08-23, see below. Sends go through Resend (see Infrastructure) instead of Zoho, specifically so opens/clicks are trackable, something the dental system never had visibility into. Leads live in a new Google Sheet, "Casdey-Gym-Leads" (owned by info@casdey.com, shared Editor with the service account below), same `Leads`/`Send Log`/`How to use` shape as the old dental sheet plus tracking columns (`Resend Email ID`, `Opened?`, `Clicked?`) and manual multi-channel columns (`Instagram/Social`, `Other Channels Contacted`, `Channel Notes`, for Davide/Abhi's own DM/community outreach, not sent by this skill). **Open items:** real open/click data needs the Resend tracking subdomain to finish verifying (added 2026-08-22 on the root `casdey.com` domain, not `mail.casdey.com`, so outreach now sends as `you@casdey.com` — DNS records live, propagation pending).

**Update, 2026-08-23: first real leads sourced, then the Routine was reworked and merged.** The sourcing Routine's first real test run (2026-08-22, manually triggered) sourced 14 real UK/EU gyms; by 2026-08-23 the sheet held 29 real leads, all drafted and marked "Ready for review", none reviewed or sent yet. Two changes landed the same day:
- **Reply detection moved out of the Routine.** It's now local/interactive-session-only (needs the real Zoho inbox creds, `ZOHO_*`), not available inside the cloud Routine at all, see below for why.
- **The Routine was split, then merged back into one, on Davide's explicit calls, in that order.** First, a second Routine ("review & send") was created with its own new, minimal cloud environment (`casdey-Outreach-Send`: Google Sheets + Resend creds, no Zoho) so Davide could review and send batches from inside the Routines UI itself, not just this chat, mirroring "Review-gated sending" in SKILL.md but living in its own routine. Then, the same day, Davide asked to merge that back into a single Routine for convenience. Both are now one Routine, **"casdey gym outreach"** (same id, `trig_018wp58QLBbeuBPbMA6Fy1sU`), running on the `casdey-Outreach-Send` environment, on a schedule (originally weekdays 09:00 CEST, since changed, see below for current cadence) plus on-demand. **This merge has a real security cost, and Davide accepted it knowingly after it was explained:** from 2026-08-22 to 2026-08-23, the sourcing Routine had no `RESEND_API_KEY` at all, a hard technical guarantee that even a successfully-injected unattended run (it does live, unsupervised web browsing to source leads, a real prompt-injection surface) could never send anything for real. The merged Routine's single environment now holds the sending credential alongside that same web-browsing capability, so "never send without a human reviewing first" is enforced only by the Routine's own prompt (a strict Part A/sourcing-and-drafting-always vs. Part B/review-and-send-only-on-live-human-request split), not by the credential being physically absent. Full detail, including how to split it back apart if that guarantee is ever wanted again, is in `.claude/skills/gym-outreach/SKILL.md` under "Why this replaced the dental skill" and "Routine status". The old sourcing-only environment (`casdey-Outreach`, with leftover dental-era `ZOHO_*` vars) still exists but is now unused.

**Volume raised to ~50/day, 2026-08-23.** After the first real batch (5 sends, see below), Davide's explicit call: the go-to-market plan's original ~5/day Week 1 pace is too slow to read how the gym niche is performing, so the target moved to ~50 reviewed/sent per day, aiming for ~350 over the coming week. The review-gated system itself is unchanged, only batch size: Part B's review-batch target went from ~5 to ~50, Part A's sourcing cap from 15 to 60 new leads/run, and its skip-sourcing threshold from 50+ to 150+ unsent rows, in both `.claude/skills/gym-outreach/SKILL.md` and the live Routine's own prompt (updated directly via the RemoteTrigger API, not just the repo file). **Resolved same day:** the Routine originally fired weekdays-only (09:00 CEST, Mon-Fri), capping at 250/week, short of the 350/week target; Davide asked for weekend runs added, so the schedule became daily (`0 7 * * *` UTC, 09:00 CEST), giving 7 × 50 = 350/week exactly. Davide then asked to move the fire time earlier, to 07:00 CEST, so a batch is ready before he wakes up; the schedule is now `0 5 * * *` UTC (07:00 CEST), daily.

**Dental history (for reference, system retired):**
- A lead list of UK + EU dental practices exists in a Google Sheet ("Casdey-UK-Dental-Leads", owned by info@casdey.com), sourced from practice websites and directories (not guessed patterns), with owner/principal names researched for independents where confidently sourced.
- Outreach ran as a fully automated daily system (a Claude Routine, see `.claude/skills/cold-outreach/SKILL.md`), not a manual batch send: it sourced leads, drafted, sent via Zoho, detected replies, and logged everything to the sheet's `Send Log` tab, unattended, once per day, capped at ~100 sends/run.
- As of the last recorded batch (2026-08-13): 87 total sends across three batches, covering UK, Ireland, Netherlands, Germany, France, Belgium, Portugal and Spain. Full batch-by-batch history is in the retired skill's `SKILL.md`, not duplicated here.
- **2026-08-15: zero-reply diagnosis, volume paused, CTA changed.** Live `Send Log` tab checked directly: 216 sends since 07/31, 0 genuine replies, only 2 hard bounces, and Davide confirmed test sends land in the main inbox, not spam, so this reads as a message/offer problem, not a broken pipe or a tracking gap. After reviewing a $0-customer-acquisition playbook (Nate Herk-style deck, not saved to this repo), Davide's call: stop scaling raw send volume and prove the message works on a small hand-picked batch first. This diagnosis is what eventually drove the niche pivot itself, not just a volume/CTA tweak within dental (see "Niche pivot" above).
- Lead tracking uses these statuses: Not contacted / Contacted / Replied / Interested / Committed / Dead — reused as-is for the gym sheet.
- Follow-up sequence per lead: send message → if no reply in 4-5 days → ONE follow-up → then stop. Ran automatically for dental; the gym skill keeps the same 4-5 day/one-follow-up spec but routes follow-ups through the same review gate as first-touch emails.
- Independent practices/studios are prioritized over chains — they decide faster. Carried over unchanged to gym outreach.

## Stage 2 progress — building
- Landing page (`/`) and waitlist (`/waitlist`) were built and were live in production at `casdey.com`; both are now mid-pivot, see "Niche pivot" above for the current routing and copy state. Brand v3 ("Iron & Brass", dark palette) applied 2026-08-19, replacing the v2 light/teal system this bullet originally described. Copy still follows the free-first-week-only rule (no price shown, per the outreach copy's own no-price-in-first-touch logic).
- Waitlist storage is a real Supabase table, `waitlist_signups`, RLS on with only the server-side service role able to read/write. Verified end-to-end on 2026-08-13 with a real test signup (row written, both notification emails sent), and again after the production deploy. **Region correction (found 2026-08-15):** the project's actual region is **eu-west-1 (Ireland)**, not eu-central-1 (Frankfurt) as earlier assumed here and in some marketing copy — still EU, so the residency promise holds, but any "stored in Frankfurt" wording is inaccurate and should say Ireland.
- The SaaS product (`/app`) has a first working build: accounts (email/password + Google, both fully wired end-to-end as of 2026-08-15 — see below), a trial → Free → Premium billing model (see `web/src/lib/plan.ts`), CSV patient import (Dentally stubbed pending real credentials), dormant-patient detection, a manual price list per practice for revenue estimation, and email re-engagement campaigns (now sendable through Resend with per-practice reply-to, not just Zoho — see below). Full detail, verified checks, and the manual steps still needed are in `web/SAAS_HANDOFF.md` and `web/SAAS_ROADMAP.md`. It exists in the same deployed app as the marketing pages. **As of 2026-08-17 the env vars `/app`'s email+billing paths need are set in Vercel Production and production has been redeployed, so `/app` now serves at casdey.com** — `NEXT_PUBLIC_SUPABASE_*`, the full `STRIPE_*` set including live keys, `RESEND_API_KEY`, `CASDEY_SENDING_ADDRESS`, and `CRON_SECRET`. Email/password auth is confirmed working in production (a malformed `NEXT_PUBLIC_SUPABASE_URL` in Vercel — it carried a `/rest/v1` suffix, the documented gotcha — was found and corrected 2026-08-17; the Supabase clients now reject that suffix outright), and Supabase Auth's Site URL, still `localhost:3000` with an empty redirect allowlist, was fixed to `https://casdey.com` + a `casdey.com/**` allowlist so OAuth and email links resolve in prod. **Two things still gate calling `/app` fully live (as of 2026-08-17):** (1) the Google OAuth consent screen is still in Testing, so real Google sign-ins may be limited to test users until it is published/verified; (2) the Calendar-sync and WhatsApp channels are **inert in production** — their env vars (`GOOGLE_CALENDAR_*`, `CALENDAR_TOKEN_KEY`, `TWILIO_*`, `ANTHROPIC_API_KEY`) are deliberately NOT in Vercel, so only email, billing, and casdey-only booking actually run in prod, despite both channels being live-verified locally. **Superseded by the niche pivot (see "Niche pivot" above):** WhatsApp is now removed from the codebase entirely rather than merely gated, Calendar sync stays a deliberate V1-scope deferral (not just a missing env var) pending the same OAuth verification, and `/app`'s liveness changed repeatedly since through the unpublish, the local gym rebuild, and the 2026-08-23 relaunch. The Niche pivot section is the current source of truth for what is actually live.
- **Google sign-in (2026-08-15):** fully wired, both sides. App code (`signInWithOAuth` + the `/auth/callback` code exchange) already existed; completed the external config — created a Google Cloud OAuth client (`casdey web`, under `info@casdey.com`) and enabled it in Supabase → Auth → Providers → Google. Confirmed "Enabled" in the Supabase dashboard. Caveat: the Google OAuth consent screen is still in "Testing" mode (only added test users can complete sign-in) — this remains a live-gate for real prospects. Verification prep is partly done (2026-08-17): the consent screen is filled in (homepage, `https://casdey.com/privacy`, authorised domains `casdey.com` + the Supabase project domain), and the sensitive `calendar.events` + non-sensitive `calendar.freebusy` scopes are declared on Data Access. Deliberately NOT published/submitted yet (publishing before submitting shows an "unverified app" warning to everyone). Remaining to submit: a demo video of the Calendar-connect flow + a square 120×120 logo, then publish + submit for verification (review is days-to-weeks). Note the booking audit's recommendation to narrow the requested scope to `calendar.app.created` before submitting — see `web/SAAS_ROADMAP.md`.
- **Campaign email via Resend (2026-08-15):** the Zoho-only send path (couldn't set a per-practice reply-to, and tripped Zoho's abuse limits when tested — see `web/SAAS_HANDOFF.md`) is fixed. Created a Resend account, verified `mail.casdey.com` as a sending domain (a **subdomain**, deliberately, so Zoho's own MX/SPF/DKIM on the root `casdey.com` are untouched), added the 3 required DNS records at GoDaddy, and set `RESEND_API_KEY` + `CASDEY_SENDING_ADDRESS` locally. Confirmed locally that campaign email now routes through Resend instead of Zoho. `RESEND_API_KEY` and `CASDEY_SENDING_ADDRESS` are now also set in Vercel Production (2026-08-17) and production has been redeployed; no real campaign send has been end-to-end tested in production yet.
- **AI-assisted campaign drafting: built, then deliberately dropped (2026-08-15).** Davide decided against embedding AI drafting for a task this small — it's a standing per-use cost for a feature nobody asked for. The language selector and per-practice price list it would have used are kept; only the "draft with AI" option itself was removed. Not on the roadmap to revisit unless the cost/demand picture changes.
- **WhatsApp channel with an AI reply loop (built 2026-08-15).** A second messaging channel alongside email: one shared casdey WhatsApp number (via Twilio) for every practice, Claude Haiku holding the actual back-and-forth once a patient replies (capped conversation history, a hard per-conversation reply limit before a human takes over), and a `mark_booking_requested` tool call that hands off to staff on clear booking intent. Everything on casdey's side of the Twilio boundary (signature verification, idempotency, conversation state, the AI call, STOP-style opt-out) is verified working; actual outbound sending is still blocked on Twilio's trial-account limits (Content Templates and custom webhook config both gated behind an upgraded account), deliberately deferred until the V1-publish decision. **Real bug found and fixed 2026-08-16:** patient phone numbers were never converted to the E.164 format Twilio requires (a practice's CSV export writes local format, e.g. `07700 900123`), breaking both outbound sends and inbound reply routing for standard UK/EU numbers — added `libphonenumber-js`, normalises to E.164 at CSV import time using the practice's own country. Full detail in `web/SAAS_ROADMAP.md` item #2.
- **Casdey-owned booking loop with Google Calendar sync (built 2026-08-15, Google OAuth wired and live-verified 2026-08-16).** The calendar write-access half of "Guarantee mechanics" above: a patient gets a self-serve booking link (`{{booking_link}}`, threaded through campaign templates, the sender, and self-test) that reads the practice's connected Google Calendar for free/busy, lets them pick an open slot, and books it straight into both casdey and that calendar, no staff step. Practices connect their own Google Calendar from Settings → Booking (OAuth, tokens encrypted at rest); booking still works without one connected, it just cannot see appointments made outside casdey. Cancelling from the patient's own manage link removes the calendar event and reverts their status correctly. **Live-tested end-to-end 2026-08-16** against a real Google account: connected a calendar, booked a real slot, confirmed the event landed on Google Calendar, cancelled it, confirmed it was removed. Full detail in `web/SAAS_HANDOFF.md`. Real PMS-diary write (Dentally/EXACT/R4) stays out of scope, same partner-API wall as the import stub — Google Calendar is the one real diary casdey can reach today.
- **The profit-or-nothing guarantee is now real, not just copy (2026-08-15).** A practice gets exactly one lifetime 30-day guarantee window: its first campaign started on or after its first real (non-trial) Premium payment. If estimated revenue recovered in that window falls short of what was actually paid, the practice can self-serve a full refund of that window's payments from `/app/settings/billing` — one click, no review, safe specifically because the window can never be re-armed. Locked with Davide before building: threshold is revenue-recovered vs. paid (reusing #7's existing formula), refund scope is everything paid in the window (not one invoice, not open-ended), and the claim is fully self-service. Full detail in `web/SAAS_ROADMAP.md` item #8. **Verified end-to-end 2026-08-16** with a real (test-mode) Stripe checkout and refund, which surfaced and fixed two bugs that had made the refund silently non-functional since it was built: the payment window's lower bound was the campaign's start time rather than the first Premium payment (so that triggering payment was always excluded from "paid"), and the Stripe webhook fetched invoices without `expand: ["payments"]`, leaving no payment/charge id for a refund to target. Both fixed. **Hardened further 2026-08-17** after the stress-test audit (see the audit bullet below): an unset appointment value can no longer auto-qualify a full-window refund (it now routes to a `needs_review` state instead of a one-click claim), and the paid/refunded set is bounded to the guarantee window's own billing period so an extra pre-window month a slow-to-launch practice accrued is no longer over-refunded.
- **Client self-test of the outreach (2026-08-15), email side.** A practice can send a campaign to itself before approving it for real patients: "Send me a test" on the campaign review page sends the exact composed message to the signed-in user's own inbox, real reply-to, real working unsubscribe link, only the subject marked so it is not mistaken for a genuine patient reply. Rides the same send pipeline as a real message via one reusable synthetic per-practice patient (`patients.is_test`), excluded from every real-facing number (dashboard stats, patient list, campaign audiences, CSV export). Full detail in `web/SAAS_ROADMAP.md` item #4. Same production gate as the rest of `/app`. Covers email; WhatsApp self-test shipped the same day alongside the WhatsApp channel itself (see the WhatsApp bullet above), and a patient can now also test the in-product booking step for real, via the self-serve booking link (see the booking-loop bullet above).
- **In-app support widget (2026-08-15).** A help launcher bottom-right of every `/app` page opens a searchable panel of curated answers about using casdey (importing, dormancy, sending, the self-test, plans, the guarantee, revenue, rebookings, unsubscribes, data/GDPR), with a plain `mailto:info@casdey.com` fallback when it cannot answer. **Deliberately not AI-backed**, decided with Davide, same standing-cost reasoning that dropped the AI drafting in #1: a curated FAQ does this job without a per-use LLM cost or the `ANTHROPIC_API_KEY` dependency. Full detail in `web/SAAS_ROADMAP.md` item #5. UI only, no schema and no new env vars, but still lives behind the same production gate as the rest of `/app`.
- **Pre-launch stress-test audit + fixes (2026-08-17).** An 8-agent read-only audit (one specialist per domain: auth, billing/guarantee, GDPR, import, WhatsApp, booking, build/config, marketing) ahead of the V1 go-live. Core engineering held up — no auth bypass, RLS on all tables, `tsc`/`lint`/`test`/`build` all green — with risk concentrated in four seams: production config, silent-failure handling on external APIs, the legal documents, and the guarantee's economics. Seven launch-blocker fixes shipped in commit `f30f61b` (pushed/deployed 2026-08-17), all code-only, no schema migration: import batch-poisoning (a single shared/duplicate email no longer drops a whole 500-row batch — dedup on the conflict key + row-by-row retry that reports the bad record) plus strict-UTF-8/Windows-1252 decoding and surfaced CSV parse errors (accented EU names no longer import as mojibake); the guarantee hardening noted above; a real password-reset flow (`/reset-password` + `/api/auth/reset-password` + a "Forgot password?" link — there was none before); an atomic cron send-claim so overlapping drains can't double-email a patient; `siteUrl()` now throws in prod instead of silently using localhost and the Supabase clients reject a `/rest/v1` URL; and em-dash removal from Google Calendar event titles. **Deferred audit items** (before enabling the gated channels): Calendar free/busy fails *open* → double-booking risk, the Testing-mode 7-day refresh-token death, and narrowing the calendar scope; the WhatsApp reply-loop fixes (malformed first AI turn, non-atomic reply-cap race, oldest-not-newest history, English-only STOP, no output moderation); and the GDPR document corrections (the signed DPA falsely denies any EEA-boundary transfer and omits the US sub-processors Google/Twilio/Anthropic; `/privacy` still says "casdey does not hold any patient data yet" and has unfilled controller-identity TODOs). All tracked in `web/SAAS_ROADMAP.md`. **Partially resolved by the 2026-08-21 gym rebuild, confirmed 2026-08-23:** `/privacy` and `/terms/processing` are now gym-worded, and the DPA's sub-processor list (Supabase/Vercel/Stripe/Zoho-Resend) is accurate for the current email-only, no-Calendar V1 scope, correctly omitting Google/Twilio/Anthropic (see "Niche pivot" above). The one gap still open on those two pages: the controller legal identity and postal address TODOs.
- Two Claude Code sessions worked this in parallel deliberately (2026-08-13): one on marketing pages/deploy, one on the SaaS build. Both stayed additive (new files/routes only, zero edits to each other's files), which is what made same-repo, same-branch parallel work safe — no folder copies or extra branches needed.
- GitHub repo: `07davidelongo-eng/casdey`, branch `main`, connected to Vercel.

## Outreach copy conventions
The niche-specific opener/pain-point language below is dental-era and now historical (the dental system is retired, see Stage 1); the current gym cold-email copy rules live in `.claude/skills/gym-outreach/SKILL.md` under "The cold email". The universal conventions (plain text, em-dash rule, sign-off format) still apply to both.
- The key opener question (dental): "of the day-to-day admin your practice software handles, what's the one thing it doesn't do well?"
- Guarantee wording: "if it doesn't recover more than it costs, you don't pay."
- Cold emails are sent in plain text — no logo, no image attachments (spam risk).
- `casdey.com` is now live (deployed 2026-08-13, see Infrastructure below). The casdey.com/waitlist link and the beta offer from "Offer evolution" above (free week of Premium once the software ships → Free plan → lifetime £50/€59 discount if they upgrade, framed explicitly as feedback-seeking, not a sales pitch) carry over unchanged into the gym cold email. The next copy flip, from "once the software ships" to "it's live now," is gated on V1 launch and not yet due.
- Supporting docs already created: a 4-email outreach sequence and a one-page Service Agreement (used once a prospect says they're interested, sent for signature).
- **Never use em dashes (—) as punctuation** (as a substitute for commas/parentheses/asides) in any casdey copy — cold emails, follow-ups, this file, anything. Use commas or separate sentences instead. Normal hyphens in compound words (follow-up, list-building, drop-off) are fine and unaffected by this rule.
- Email sign-off format: `Davide @casdey` (single line, no line break between name and company, no "Best,"/"Regards," preamble).

## Infrastructure already in place
- Domain: casdey.com, registered at GoDaddy.
- Email: Zoho Mail (free plan), not Google Workspace — blocked there due to no Partita IVA yet. MX/SPF/DKIM configured and verified.
- Two users: davide@casdey.com (me) and abhi@casdey.com (business partner).
- info@casdey.com is a shared Group (not an alias) — mail delivered to both our inboxes, both can send from it. Zoho treats it as a group, not a mailbox: it has no API/IMAP account of its own, so programmatic access goes through davide@casdey.com's account using send-as.
- Google Workspace signup for casdey.com is still blocked (no P.IVA), but info@casdey.com does have a plain Google Account (likely a byproduct of the earlier interrupted signup attempt) — this is what the `gws` CLI and Google Sheets/Drive access authenticate as. A separate Google service account (`casdey-routine@casdey-gws-cli.iam.gserviceaccount.com`) exists for Sheets access from outreach tooling, see `.claude/skills/gym-outreach/SKILL.md` (current) and `.claude/skills/cold-outreach/SKILL.md` (retired). It can operate on sheets shared to it but cannot create new ones itself (no Drive storage quota of its own) — a new lead sheet has to be created by a human first, then shared Editor with it, same as both lead sheets were set up.
- **Stripe: activated for live payments (2026-08-17, confirmed via dashboard, done in a session not otherwise recorded).** Account `acct_1Tz17NDGwemFDmSP` — Account status shows no active tasks and capability "Payments: Active" (plus Payouts/Transfers), business details filled in, and a real EUR bank account (Revolut, ••••8098) attached for payouts. Test-mode prices for all four plan variants were already created (see `web/SAAS_HANDOFF.md`). **Live-mode setup completed the same day:** `stripe-setup.mjs` deliberately refuses to run against a live key (live pricing is meant to be a manual dashboard decision, not a script side effect), so the live `casdey` product, its 4 prices, and the 2 lifetime-discount coupons were created by hand in the Stripe dashboard to match the test-mode spec. A live webhook endpoint ("casdey production", `https://casdey.com/api/stripe/webhook`) was also created, listening to exactly `checkout.session.completed`, `customer.subscription.created`/`updated`/`deleted`, `invoice.paid`, and `invoice.payment_failed`. All resulting `STRIPE_*` env vars (secret key, 4 price ids, 2 coupon ids, webhook signing secret) are now set in Vercel Production — see the Vercel bullet below for what that does and doesn't mean yet.
- Resend: account created 2026-08-15, sending domain `mail.casdey.com` verified (region eu-west-1/Ireland, matching Supabase). API key is Sending-access-only, not Full access (confirmed 2026-08-22: it 403s on domain-management endpoints, e.g. enabling tracking, those need the dashboard UI, not this key). Set locally and, as of 2026-08-17, also in Vercel Production (`RESEND_API_KEY`, `CASDEY_SENDING_ADDRESS`). **Second domain added 2026-08-22: root `casdey.com` itself**, specifically for gym cold outreach (`.claude/skills/gym-outreach/`) so it sends as `you@casdey.com` rather than a subdomain address, which reads more like a real person for cold email. This is safe alongside Zoho's existing mail on the same root domain: Resend isolates its own DKIM (`resend._domainkey.casdey.com`, a distinct selector) and SPF (`send.casdey.com`, its own MX+TXT, isolated from root) onto dedicated subdomains it manages itself, so the existing root SPF/MX/DKIM Zoho relies on for davide@/abhi@/info@ is untouched, verified directly against live DNS before and after. Tracking (open/click) also enabled on this domain via a `links.casdey.com` CNAME. All 4 DNS records added at GoDaddy 2026-08-22 (browser-driven, Davide's own logged-in sessions); Resend showed "Pending" verification right after, pending propagation. Outreach sends via `CASDEY_OUTREACH_SENDING_ADDRESS` (a friendly local-part on this new `casdey.com` domain, set locally only so far, not in Vercel — outreach doesn't run through the deployed app), kept distinct from the product's own `no-reply@mail.casdey.com` campaign sender. Used four ways total now: casdey's own campaign sends go through `RESEND_API_KEY` directly on `mail.casdey.com`, Supabase Auth's transactional emails route through Resend via custom SMTP on `mail.casdey.com` (see below), and gym cold outreach sends on the new root `casdey.com` domain with its own tracking. **As of 2026-08-17 Supabase Auth's transactional emails (signup confirmation, password reset) route through Resend via custom SMTP** (`smtp.resend.com:465`, user `resend`, sender `no-reply@mail.casdey.com`, key entered in the Supabase dashboard). This fixed a real blocker: Supabase's free-tier shared SMTP was silently failing to deliver confirmation emails, so no email/password signup could complete. Supabase auto-bumped its auth-email rate limit to 30/hr on enabling custom SMTP (raise before real volume).
- GitHub: `07davidelongo-eng/casdey`, branch `main`.
- Vercel: set up 2026-08-13. Team/project both named `casdey`, Root Directory `web`, `main` = production. Deployed at `casdey.vercel.app` and at the custom domain `casdey.com` (see below). Production env vars started as the waitlist-safe set only (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, the `ZOHO_*` vars, `WAITLIST_NOTIFY_TO`, `CASDEY_FROM_ADDRESS`, `NEXT_PUBLIC_SITE_URL`); **as of 2026-08-17, Davide added the full remaining set needed to go live** — `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, the complete `STRIPE_*` set (live secret key, 4 prices, 2 coupons, webhook secret), `RESEND_API_KEY`, `CASDEY_SENDING_ADDRESS`, and `CRON_SECRET`. `web/vercel.json` (added 2026-08-17) registers a cron hitting `/api/cron/send`, **dropped from hourly to once daily (`0 3 * * *`) on 2026-08-19** because the team turned out to be on the Hobby plan, which caps cron jobs at once/day (see the outage note below); revert to hourly if/when the team is confirmed on Pro. Production has since been redeployed and `/app` serves at casdey.com again; the remaining "fully live" gates are the Google OAuth consent screen still in Testing and the deliberately-absent Calendar/WhatsApp env vars (see the `/app` bullet under Stage 2 for both). **Billing status is now uncertain, not confirmed Pro as earlier entries here assumed.** This file previously said the team had a card on file (Visa ••••7600) but was still on a Pro Trial running 13–27 Aug 2026, expected to auto-convert on the 27th. That assumption is now doubtful: the Hobby-plan cron error hit on 2026-08-19 is the kind of restriction a Pro account, trial or paid, shouldn't hit. Nobody has confirmed the actual plan on the Vercel billing page since, check there directly rather than trust either claim. `web/SAAS_HANDOFF.md` had the same stale Pro Trial claim, corrected there too.
- **Deploy pipeline outage found and fixed, 2026-08-19.** The GitHub↔Vercel git integration had silently gone "sourceless": Vercel's own project record still showed it as connected (`vercel git connect` even reported "already connected"), but the webhook binding was dead, sometime between the last real deploy on 2026-08-17 and 2026-08-19. Every push to `main` in that window, several of them earlier the same day, including the unpublish redirects and the gym `/waitlist` rework, was not actually deploying despite pushing cleanly to GitHub, `casdey.com` kept serving 2026-08-17's code the whole time. Confirmed by fetching the live site directly, not through Google, whose stale cache is what first surfaced the problem. Fixed with `vercel git disconnect` immediately followed by `vercel git connect`, both run from the repo root, not `web/`: the CLI's git-repo detection doesn't reliably walk up from the Vercel Root Directory subfolder and fails there with a false "No local Git repository found". Verified with a throwaway empty commit that auto-deployed within a minute of pushing, no manual `vercel deploy` needed. The Hobby-plan cron limit above was a second, unrelated blocker surfaced by the same investigation. **Stray artifact from troubleshooting:** an accidental empty Vercel project named `web` (distinct from the real `casdey` project) was created when `vercel git connect` was first run from the `web/` subdirectory, before the repo-root fix was found. It holds no deployments and should be deleted from the Vercel dashboard when convenient, not yet done.
- **Push discipline (Davide's standing rule, 2026-08-14):** because `main` auto-deploys to production on push, committing locally is always fine, but pushing to `main` needs fresh explicit confirmation every time, even if a similar push was already approved earlier in the same or a prior session. Enforced by `.claude/skills/push-github/`. **GitHub push and Vercel deploy are not separable right now** — Vercel has no commit-message flag to skip a deploy; the only way is a one-time "Ignored Build Step" setting in Vercel's project settings, which Davide was offered and declined on 2026-08-15 (wants things left as-is). Until that changes, pushing to `main` on GitHub *is* deploying casdey.com to production, no way around it.
- `casdey.com`: DNS moved from GoDaddy's parked-page defaults to Vercel on 2026-08-13. Two records changed at GoDaddy (`A @ → 216.198.79.1`, `CNAME www → c7edcf5d9dc7d3ff.vercel-dns-017.com.`); the existing Zoho MX/SPF/DKIM records were left untouched and email still works. SSL is auto-provisioned by Vercel. DNS propagated within minutes for this test but can take up to 48h globally.
- Supabase: project `casdey`, region **eu-west-1 (Ireland)** — corrected 2026-08-15; earlier docs said eu-central-1 (Frankfurt), confirmed wrong via the Supabase dashboard. Still EU, so residency holds, but any "Frankfurt" wording elsewhere (including marketing copy) should be fixed to "Ireland". Used so far for the waitlist (`waitlist_signups` table) and now the SaaS's own data too — migrations through `0010` (booking) applied to the live project (see `web/SAAS_ROADMAP.md`). Free tier. Auth transactional email now goes through custom SMTP → Resend (see the Resend bullet above), not Supabase's shared sender.

## Legal/tax context (Italy)
- No Partita IVA yet — first clients will be invoiced via prestazione occasionale.
- Partita IVA to be registered once revenue is consistent (regime forfettario).

## Open questions before building the SaaS
Raised 2026-08-13. Most were answered the same day and the first SaaS build reflects the answers (see `web/SAAS_HANDOFF.md` for the full assumption list):
- MVP scope: **answered** — CSV/spreadsheet upload is the working, universal path; Dentally is stubbed (interface implemented, reports "not connected" until real API credentials exist), not a guess. A **manual** price list per practice now exists (2026-08-14, Settings → Service prices) and feeds the revenue-recovered estimate on the dashboard, which the guarantee refund (2026-08-15, see below) is judged against. Calendar write-access is now built (2026-08-15/16, see the booking-loop bullet above) — casdey can book and cancel appointments in a practice's connected Google Calendar. Automatic price-list read-access (from the practice's own software, rather than the existing manual Settings → Service prices entry) is still **not yet built**.
- Auth: **answered** — Supabase Auth, same project as the waitlist DB (email/password + Google, both fully wired as of 2026-08-15).
- Billing flow: **answered, then revised 2026-08-14 to match the beta→V1→V2 offer.** The SaaS now implements trial → free → premium (see `web/SAAS_HANDOFF.md` and `web/src/lib/plan.ts`): a new signup gets a 7-day Premium free week with **no card taken** (the trial is casdey-managed, not a Stripe trial), then drops to the Free plan (can import + see dormant patients, cannot send), and upgrades to Premium via Stripe (card taken then) with a lifetime early-adopter discount applied automatically. Test-mode prices and discount coupons created; not live in production yet (see Infrastructure). The earlier "card at signup, auto-charged after trial" design was replaced by this.
- Messaging channel: **answered** (not originally asked, but assumed and worth recording) — email only in v1, no SMS/Twilio. The first campaign a practice sends requires explicit approval before it goes out.
- Still open: how the free-first-week promise gets fulfilled operationally for any real signup that arrives before the product is solid. `web/SAAS_HANDOFF.md` calls this out explicitly: the gap stays manual, Davide handles it personally if someone signs up early.
- Partly open: the Free plan referenced in "Offer evolution" above now exists in code (2026-08-14) with a working default limit, import and see dormant patients but cannot send campaigns, which is the pull toward Premium. What is still a product decision is whether Free needs *more* limitations ("A LOT" per Davide, e.g. a patient-count cap or a capped dormant-list preview); those are easy to add in `web/src/lib/plan.ts` `capabilities()` when decided, before V1 messaging goes out.
